# 프로젝트 규칙 (반드시 준수)

## 🔒 버전 & 업데이트 내역 (절대 잊지 말 것)

`index.html`을 수정할 때는 **항상** 아래를 함께 처리한다:

1. **버전 번호는 언제나 무조건 올린다** (예외 없음)
   - `<title>` 의 버전
   - 상단 브랜드 툴팁 `.app-version-tooltip` 의 버전·날짜
   - 업데이트 내역 데이터의 최신 항목 `version` (내역을 쓰는 경우)
   - 세 곳의 버전이 서로 어긋나지 않게 동기화한다.

2. **업데이트 내역(CHANGELOG) 작성 기준**
   - **작성해야 함**: 기능 추가, 기능 수정 등 **사용자가 겪는 경험이 조금이라도 달라지는** 모든 변경.
   - **생략 가능**: 사용자에게 티 안 나는 단순 내부 버그 수정. (단, 이 경우에도 **버전 번호는 반드시 올린다**.)
   - 내역을 쓸 때는 `CHANGELOG` 배열 **맨 위**에 새 항목을 추가하고, 이전 "최신" 표기는 제거한다.
   - 항목 형식: `{ version, date, time, tag: 'feat'|'fix', items: ['[영역] 사용자 관점 설명', ...] }`
   - `items`는 코드 용어가 아니라 **사용자가 이해할 수 있는 말**로 쓴다.

### 위치 참고
- **업데이트 내역 데이터**: `index.html` 맨 아래 `<script type="application/json" id="pl-changelog-data">` 안의 JSON 배열.
  ⚠ **v9.172.0부터 앱에는 최근 60개만 싣는다.** 그보다 오래된 것은 `data/changelog-archive.json` 으로 옮겼고
  내역 창의 [예전 내역 더 보기] 를 눌렀을 때만 받아온다(내역만 276KB = 문서의 11%였다 → 내려받기 637→575KB).
  **60개를 넘기면 넘친 만큼을 아카이브 맨 앞으로 옮긴다**(지우지 않는다). 검사: `changelog-test`.
  (v9.134.0에서 옮겼다 — 자바스크립트 리터럴로 두면 230KB를 앱 열 때마다 파싱해서 부팅이 느려진다.
   화면에서 읽는 건 `getChangelog()` 하나뿐이고, 내역 창을 열 때 1회만 `JSON.parse` 한다.)
  ⚠ **JSON이라 작은따옴표·주석·후행 쉼표를 쓸 수 없다.** 새 항목은 배열 **맨 앞**에 넣고,
  직전 항목의 `"time"` 은 `""` 로 바꾼다. 본문에 `</script>` 가 들어가면 `<\/script>` 로 escape.
- 버전 툴팁: `.app-version-tooltip` (좌상단 "프롬프트 랩" 브랜드)
- 현재 최신 버전: **v9.189.0**

## 🎨 테마 변수는 `<html>`·`<body>` 두 곳에 걸린다 (v9.165.2 / v9.168.1)

`data-theme` 은 **head 부팅 스크립트가 `<html>` 에**(FOUC 방지), **`initTheme`·`toggleTheme` 이 `<body>` 에**
건다. 둘이 어긋나면 안쪽(`<body>`)이 이긴다 — 여기서 사고가 두 번 났다.

1. **v9.165.2 이전**: 토글이 `<body>` 만 바꿔서 `<html>` 이 light 로 굳었다. 다크 값이 `:root` 에만
   있어 `[data-theme="dark"]` 규칙이 없었으므로, body 를 dark 로 바꿔도 html 의 라이트 값을
   그대로 물려받았다 → **라이트에 한 번 들어가면 다크로 못 돌아왔다.**
   → `_plApplyTheme()` 이 두 곳을 함께 맞추고, 팔레트 선택자를 `:root, [data-theme="dark"]` 로.
2. **v9.168.1**: 그 팔레트 확장 때문에, 모바일 전용 덮어쓰기(`@media (max-width:1024px)` 안의
   `:root { --glass-bg: 불투명 }`)를 **`<body>` 선언이 이겼다** → 폰 다크에서 메뉴·모달이
   42% 반투명이 돼 **창 뒤가 그대로 비쳤다**(backdrop-filter 는 모바일에서 꺼 두므로 흐림도 없음).

**규칙: 테마 변수를 덮어쓰는 미디어 쿼리·`@supports` 블록은 선택자를 팔레트와 똑같이 쓴다**
(`:root, [data-theme="dark"]` / `[data-theme="light"]`). `:root` 만 적으면 조용히 진다.
검사: `glass-test`(폭 5종 × 테마 2종에서 html·body 변수 일치 + 모바일 불투명도 ≥ 0.9
+ **픽셀 실측** — 메뉴 뒤 화면을 빨갛게 칠하고 메뉴 여백 픽셀에 붉은 기가 도는지 본다).
⚠ **'비쳐 보인다'를 스크린샷 눈대중으로 판정하지 말 것.** 메뉴 *바깥* 글자와 메뉴 *자기* 글자를
안이라고 착각하기 쉽다(실제로 한 번 오진했다). 반드시 뒤 배경을 바꿔 놓고 픽셀 차이로 잰다.

## 📱 반응형은 '폭'이 아니라 '누를 수 있는가'가 문제였다 (v9.171.0 실측)

폭 9종(320~1920) × 화면 5종을 전수로 재 봤더니 **가로로 삐져나오는 곳은 0건**이었다.
스와이프용 옆 패널이 화면 밖에 있는 건 정상이다(`#mobile-panel-track` — 잘라내는 조상이 있으면 제외하고 세야 한다).

진짜 문제는 **터치 타깃**, 그중에서도 이것:
- **아이콘 폰트(Font Awesome)는 CDN 에서 `media="print" onload` 로 렌더 비차단 로드한다.**
  오프라인·느린 망에서 안 오면 `<i>` 가 0px 이 돼 **상단바 버튼이 22x18 로 쪼그라든다** — 폰에서 못 누른다.
  → 아이콘 버튼에는 **`min-width`/`min-height` 를 박아 둔다**(아이콘이 없어도 자리가 남게).
- 손가락 기기 판정은 **폭이 아니라 `@media (pointer: coarse)`** 로 한다(큰 태블릿도 잡힌다).
- 작고 동그란 버튼(`.help-dot`)은 **크기를 키우지 말고 `::after` 로 히트 영역만** 넓힌다 — 키우면 옆이 밀린다.
- Anima 칩은 **세로 패딩만** 키운다. 가로를 건드리면 가슴 6단계가 다시 두 줄이 된다(칩 폭 실측 항목 참조).

검사: `responsive-test`(폭 11 × 화면 5, **아이콘 폰트를 일부러 막고** 돌린다 — 그래야 위 사고가 재현된다).

## 📖 폴더블은 '폭 목록'과 '새 창으로 재기' 둘 다에서 샜다 (v9.174.0 — 사용자 신고)

사용자가 갤럭시 Z 폴드 8 에서 **"펼쳤는데 레이아웃이 안 바뀐다"**. 원인이 두 겹이었다:

1. **문턱이 안 맞았다.** Anima 2단 전환이 `@media (min-width:1000px)` 인데
   **폴드는 펼쳐도 884px** 이라 영영 1단이었다. → **820px** 로 낮췄다.
   왜 하필 820인가: 더 낮추면 칸이 좁아져 **가슴 6단계가 두 줄로 밀린다.**
   실측 — 820px 에서 왼쪽 칸 **385px**, 칩 276px + 간격 25px = **301px** 필요 → 한 줄로 들어간다.
   더 낮출 일이 생기면 **계산하지 말고 `fold-test` 를 돌려 다시 잴 것.**
2. **검사 폭 목록에 850~960 이 통째로 없었다** (320·360·390·412·480·768·1024·1280·1920).
   폴드 펼친 폭이 정확히 그 구멍에 있었다. → 850·884 를 넣었다.

### ⚠ 더 근본적인 것 — 폭마다 '새 창을 여는' 검사는 폴더블을 못 잡는다
`responsive-test` 는 폭마다 새 컨텍스트를 만들어 잰다. 그건 **"그 폭으로 앱을 새로 열면"** 이지
**"열려 있는 앱의 폭이 바뀌면"** 이 아니다. 폴더블·창 크기 조절·화면 회전은 전부 후자다.
→ `fold-test` 는 `setViewportSize` 로 **실제로 접었다 폈다** 한다(3회 왕복 포함).
**리사이즈 때 다시 계산해야 하는 것을 새로 만들면 여기에 한 줄 더할 것.**

## 📏 하단바 위치는 **사용자가 고른다** — 자동 감지를 걷어냈다 (v9.177.0)

폴드 겉화면 가로 **900×390** 실측: 상단바 50 + 하단바 **190** = 화면의 **62%** 가 컨트롤,
내용에 남는 세로 **150px**. 가로에서는 세로가 굶는데 하단바는 가로를 남기고 있었다 —
아이콘 8개는 가운데 몰려 양옆 300px 씩 비었고, 그룹 버튼은 하나당 140px, 실행은 876px 짜리였다.
**남는 축으로 옮기면 그대로 이득이다.**

### ⚠ 자동 감지로 두 번 틀렸다 — 그래서 없앴다
- **v9.174.0**: 2단 문턱 `min-width:1000px` 에 폴드 펼친 폭 **884px** 가 안 걸림.
- **v9.175.0**: 레일 조건 `max-height:560px` 에 펼친 가로 **884px** 가 안 걸림.

두 번째가 특히 교훈이다. 사용자가 옆으로 옮기고 싶어 한 진짜 이유는 '화면이 낮아서'가 아니라
**2단에서 하단바가 왼쪽 칸과 중복이라서** 였다. 그건 기기 크기로 알아낼 수 있는 게 아니다.
→ **화면 크기로 사용자 의도를 맞히려 들지 말 것.** 크기로 알 수 있는 건 '들어가느냐'뿐이다.

### 지금 구조
- 위치는 `#anima-root` 의 **`data-rail`** 하나로만 정한다 — `bottom`(기본) / `right` / `left`.
  `bottom` 이면 레일 CSS 규칙이 **하나도 안 걸린다** = 예전 하단바 그대로.
- 바 안의 **⬓ 버튼**이 `bottom → right → left → bottom` 으로 돈다. 글자가 현재 위치를 보여 준다
  (⬓ 아래 · ◨ 오른쪽 · ◧ 왼쪽). **이 버튼을 절대 `display:none` 하지 말 것** — 숨기면 되돌릴 방법이 사라진다.
- 좁은 화면이라고 임의로 하단으로 되돌리지 **않는다**(사용자 결정). '왜 맘대로 바뀌지'가 없어야 한다.
- 레일이면 `--anima-mact-h` 는 **0** 이다(세로를 안 먹는다). 실제 높이를 넣으면 결과 칸이 0이 된다.
- 설정 키 `anima_rail_side_v1` — `_IO_ETC_KEYS` 에 등록 완료.
- `mact` 는 자주 다시 그려진다 → 그릴 때마다 `_animaApplyRailSide()` 로 버튼 글자·툴팁을 맞춘다.

### 만들면서 실제로 밟은 지뢰 4개
1. **CSS 주석 안에 백틱을 썼다** → `ANIMA_CSS` 는 템플릿 문자열이라 거기서 끊겨 **앱이 통째로 안 떴다.**
   이 블록 안에서는 백틱과 `${` 를 절대 쓰지 말 것. **v9.175.0 에서 밟고 v9.177.0 에서 또 밟았다.**
   검사법: `ANIMA_CSS` 구간의 백틱 개수 == 0.
2. **`.anima-run { min-width:180px }`** 이 96px 레일에서 넘쳐 실행 글자가 잘렸다 → 레일에서 `min-width:0`.
3. **세로 flex 에서 칸들이 쭈그러들어** 그룹 버튼 6개가 14px 로 눌려 글자가 잘렸다 → `flex-shrink:0`.
   ⚠ `.anima-mact-bar` 를 `display:contents` 로 풀었기 때문에 `.anima-mact > *` 선택자로는
   **안 잡힌다**(선택자는 DOM 트리를 본다) — 하나씩 적어야 한다.
4. 처음엔 그룹 버튼이 스크롤 아래로 밀려 **안 보였다.** 레일은 세로로 길어 스크롤이 생기므로
   **자주 쓰는 것이 스크롤 없이 보이는가**가 전부다 → `order` 로 실행 → 그룹 6 → 아이콘 → 📋 → ⬓.

### 배치
표정 칩 줄은 폭을 재서 개수를 정하는 구조(`_animaFitChipRow`)라 96px 에서는 뜻이 없다 →
숨기고 「표정·포즈」 팝업으로 보낸다. 팝업은 레일 폭에 눌리지 않게 `position:fixed` 로 옆에 연다.

검사: `railpos-test`(폰 세로 · 폴드 펼침 · 겉화면 가로 **세 화면 모두**에서 3단계 순환 ·
여백 방향 · `--anima-mact-h` 0 · 그룹 버튼 안 눌림 · 팝업 폭 · **새로고침 뒤 유지** · 백업 키).
`fold-test` 는 접었다 폈다(2단 전환)만 본다.

### 하단바 옵션 줄 접기 (v9.178.0) — **위치와 독립된 축으로 둘 것**
바 맨 위 손잡이로 아이콘·표정·그룹 줄만 접고 실행은 남긴다(실측 **218 → 98px**).
사용자가 지적한 '2단에서 왼쪽 칸과 중복' 에 대한 답이기도 하다 — 2단에서는 접어 두면 겹치지 않는다.

- **`data-mact`(open/fold)** 는 **`data-rail`(위치)과 조합하지 않는다.** 숨길 대상 3줄만 적으면
  아래·왼쪽·오른쪽 어디서든 똑같이 동작한다. 조합으로 나열하기 시작하면 축이 늘 때마다
  규칙이 배로 늘고, 이 문서에 반복해 적힌 '축이 늘면 조용히 샌다'를 그대로 밟는다.
- **기본은 펼침이어야 한다.** 접힘이 기본이면 하단바를 직접 만지는 검사 4개
  (`color`·`secui`·`railpos`·`lbgrp`)가 한꺼번에 깨진다 — 그걸 맞추려 검사를 고치는 건 순서가 거꾸로다.
- ⚠ **팝업(후타·그룹)은 바의 자식이다.** 열어 둔 채 접으면 같이 사라져 '눌렀는데 아무 일도 안 남'이
  된다 → `_animaToggleMactFold()` 가 접기 **전에** 팝업을 닫고 `_animaExprOpen` 도 되돌린다.
- ⚠ 손잡이는 **접힌 상태에서도 남아야** 되돌릴 수 있다. 위치 버튼과 같은 이유로 `display:none` 금지.
- ⚠ **접기 버튼을 '자기 줄'로 만들지 말 것.** 처음에 바 맨 위에 손잡이 한 줄을 따로 뒀더니
  그것만으로 **펼친 상태의 바가 225 → 253px 로 28px 두꺼워졌다**(`color-test` 의
  '하단 바 높이가 전과 같다' 가 잡았다). 하단바 높이는 사용자가 계속 문제 삼아 온 곳이라
  **기본 상태를 두껍게 만드는 건 그 자체로 회귀**다. → 이미 있는 실행 줄 안의 버튼으로 넣어
  높이 증가를 0으로 만들었다. 접으면 **190 → 70px**.
  **하단바에 무엇을 더할 때는 반드시 `color-test` 를 먼저 돌려 볼 것.**
- 접었다 펴면 바 높이가 변한다 → `_animaSyncMactHeight()` 를 부른다(이미 `_animaAnchorScroll` 로
  감싸여 있어 보던 자리가 안 튄다). `--anima-mact-h` 를 쓰는 CSS 4곳은 자동으로 따라온다.
- 설정 키 `anima_mact_fold_v1` — `_IO_ETC_KEYS` 등록 완료.

**미리 확인해 두고 안심한 것**: `_animaFitChipRow` 는 표정 줄이 숨겨져 `clientWidth === 0` 이면
`if (!avail) return;` 로 **캐시하지 않고 빠져나간다** — 접힌 상태에서 잘못된 칩 개수가 굳지 않는다.

검사: `mactfold-test`(**위치 3 × 접힘 2 = 6조합 전부** · 실행/손잡이 잔존 · 바 높이 감소 ·
`--anima-mact-h` 동기 · **팝업 열어 둔 채 접기** · 새로고침 유지 · 백업 키).

### 남은 것 — 2단에서의 중복
사용자 지적: "왼쪽에도 설정이 있고 하단바에도 있으니 쓸데없는 공간이 많다."
맞는 지적이다 — 2단에서는 옵션 칩과 실행 버튼이 **왼쪽 칸에 그대로 다 있다.**
지금은 바를 옆으로 옮겨 190px → 96px 로 줄이는 것까지만 했다.
**아예 숨기는 선택지(4단계)는 일부러 넣지 않았다** — 숨기면 되돌릴 버튼도 같이 사라져서
복구 경로를 옵션 패널 안에 따로 만들어야 하기 때문이다. 필요해지면 그때 넣는다.

## 🪟 2단에서 '기준'은 왼쪽 칸이 맡는다 (v9.179.0 — 사용자 신고)

사용자 신고: **"왼쪽엔 큰 원본, 오른쪽엔 작은 원본 + 작은 출력."** 같은 원본이 한 화면에 두 번이었다.
게다가 그 중복이 결과 칸을 반으로 갈라 **결과가 186px 밖에 안 나왔다**(실측).

**2단에서는 비교가 이미 칸끼리 일어난다** — 왼쪽 원본(크게) ↔ 오른쪽 결과(크게).
오른쪽 안의 미니 비교는 **1단(폰)용 장치**다. 폰은 스크롤해야 원본이 보이니 옆에 작게라도 필요하지만,
2단에서는 그 이유가 사라졌는데 장치만 남아 **더 나쁜 비교로 더 좋은 비교를 밀어내고** 있었다.

- **1단** = 예전 그대로(비교 칸 안에 원본|결과 나란히).
- **2단** = 왼쪽이 기준(원본은 「이미지 넣기」에, 🔒 잠근 그림은 `#anima-lockslot`),
  오른쪽은 결과 하나뿐 → **186 → 390px**.
- ⚠ **조건 분기로 만들지 말 것.** 처음엔 '잠금이 없을 때만 원본을 숨기자'를 제안했는데,
  그러면 **잠글 때마다 결과가 반으로 줄어 화면이 튄다.** 사용자가 바로 거절했다.
  지금은 잠금 여부와 무관하게 오른쪽은 늘 결과 하나다.

### 만드는 법 — 두 벌로 만들지 않는다
프레임을 만드는 곳은 `_animaRenderResult` **한 곳뿐**이고, `_animaTwoCol()` 로 **자리만** 정한다.
같은 HTML 을 양쪽에 그려 놓고 CSS 로 감추면 '두 군데 유지'가 되어 이 문서에 반복된 사고를 밟는다.
- `.anima-col-l` / `.anima-col-r` 은 1단에서 `display:contents` 라 **DOM 에 늘 존재한다** →
  왼쪽에 자리(`#anima-lockslot`)를 두기만 하면 되고, 비면 `:empty` 로 숨는다.
- ⚠ `_ANIMA_TWO_MQ`(`(min-width:820px)`)는 **CSS 미디어 쿼리와 글자까지 같아야 한다.**
- ⚠ 클릭·꾹누르기 핸들러는 `#anima-result` 안에만 걸린다 → 왼쪽 자리에도 **똑같이 걸어 준다.**
- ⚠ 폭이 1단↔2단을 넘나들 때만 다시 그린다(`onResize` 에서 상태 비교). 매번 그리면 결과가
  많을 때 리사이즈가 무거워진다.
- `:has()` 에 기대지 않고 2단이면 `.anima-cmp.one` 클래스를 직접 붙인다(구형 엔진 대비).

### ⚠ 검사에서 헛수를 세 번 잡았다 — '중복'을 세는 방법
① `_anima.img.dataURL` 과 대조 → **넣기 카드 미리보기는 잘라내기 등으로 가공된 주소**라 안 맞는다.
② `data:image/svg` 형식으로 세기 → **잠근 그림도 같은 형식**이라 같이 세어진다.
③ 보이는 이미지 중 주소가 겹치는 것 세기 → **갤러리에도 같은 결과가 뜬다**(정상인데 중복으로 잡힘).
→ 지금은 **갤러리를 뺀 뒤 주소 겹침**을 세고, 검사 설정에서 **잠근 그림과 보는 결과를 서로 다른 그림**으로 둔다
  (실사용이 그렇다 — 옛 결과의 시드를 잠가 두고 새 결과를 본다).

### ⚠ 원본 프레임을 없앴으면 '원본을 크게 볼 길'을 반드시 남길 것
2단에서 비교 칸의 원본 프레임을 없앴더니 **원본을 크게 볼 방법이 통째로 사라졌다**
(「이미지 넣기」 미리보기는 원래 눌러도 안 열린다). `lbgrp-test`(1400px = 2단)가 잡았다.
→ 2단에서는 **「이미지 넣기」의 큰 미리보기를 눌러** 열도록 했다(1단은 손대지 않았다).
- 컨테이너(`#anima-input`)에는 **한 번만** 건다(`dataset.plLbBound`) — 다시 그릴 때마다 걸면
  핸들러가 쌓인다(`renderChips` 에서 겪은 그 사고). 클로저는 `window._animaOpenSrcLb` 로 늘 최신이다.

**검사에서 배운 것 두 가지**
- ⚠ **열리지도 않았는데 `history.back()` 을 하면 문서가 통째로 날아간다.** 그러면 다음
  `page.evaluate` 가 `ReferenceError: _anima is not defined` 로 죽어서, 원인이 엉뚱한 데 있는 것처럼 보인다
  (실제로 한참 헤맸다). → 검사에서는 `window._lbActive` 를 보고 **열렸을 때만** 되돌린다.
- ⚠ `_anima.img` 만 넣고 `_animaRenderInput()` 을 안 부르면 미리보기 `<img>` 가 없다.
  2단에서 원본을 여는 길이 그 미리보기이므로, 검사 설정에서 함께 그려야 한다.

검사: `twocol-test`(2단 중복 0 · 결과 폭 · 잠금 이동 · **잠가도 결과 크기 불변** ·
1단 무손상 · **접었다 폈다 자리 이동**).

### 👁 1단 「결과만 보기」 — 원본 프레임을 접는다 (v9.187.0 — 사용자 요청)
1단(폰)은 비교 칸이 [원본 | 결과]라 390px 실측으로 각각 **158px** 뿐이다. 접으면 결과가 **328px(2.1배)**.

- 「③ 결과 · 비교」 **제목 줄 오른쪽 끝**의 버튼(`#anima-after-toggle`). 키 `anima_afteronly_v1`
  (`_IO_ETC_KEYS` 등록 완료). **2단에서는 버튼 자체를 숨긴다** — 기준을 왼쪽 칸이 맡으므로 접을 원본이 없다.
- ⚠ **버튼을 '자기 줄'로 만들지 않았다.** 하단바에서 손잡이를 자기 줄로 뒀다가 **28px 두꺼워졌던**
  그 사고(v9.178.0)와 같은 자리다 — 결과 칸 높이는 사용자가 계속 문제 삼아 온 곳이다.
  이미 있는 제목 줄에 넣어 **높이 증가 0px**. 검사가 제목 줄 높이를 실제로 잰다.
- ⚠ **오른쪽 정렬은 빈 칸(`.anima-step-sp`)이 맡는다.** 예전처럼 버튼마다 `margin-left:auto` 를 걸면
  둘이 같이 보일 때 사이가 벌어지고, 앞 버튼(「↻ 이 결과를 원본으로」)이 숨으면 뒤 버튼이 왼쪽으로 튄다.
- ⚠ **아이콘 폰트를 쓰지 않는다.** 칸 모양을 `<i>` 두 개로 직접 그린다 — Font Awesome 이 안 오면
  0px 로 쪼그라드는 사고를 이미 겪었다(v9.171.0). 손가락 기기 히트 영역은 `::after` 로만 넓힌다
  (버튼을 키우면 제목 줄이 두 줄로 밀린다).
- ⚠ **시드를 고정해도 똑같이 접는다**(사용자 결정). '잠금일 때만 다르게'는 잠글 때마다 결과가
  반으로 줄어 화면이 튄다 — v9.179.0에서 사용자가 거절한 그 패턴이다.
- ⚠ **원본을 크게 볼 길을 반드시 남긴다**(v9.179.0 교훈). 크게 보기 목록은 **안 건드리므로** 원본이
  여전히 왼쪽 끝에 있고, 「이미지 넣기」 미리보기도 눌러서 열린다(2단에서 만든 그 길을 여기서도 연다).

검사: `afteronly-test`(접힘/펼침 · 결과 폭 2.1배 · **제목 줄 높이 불변** · 버튼 안 삐짐 ·
시드 고정해도 접힘 · 크게 보기 맨 앞이 원본 · 미리보기 열림 · 새로고침 유지 · 백업 키 ·
**2단에서 숨김** · 접었다 폈다).

### 🔌 ComfyUI 연결 카드는 주소가 있으면 「고급 설정」으로 내려간다 (v9.179.0)
사용자 지적: "한 번 설정하면 건드릴 일이 거의 없는데 상단 자리만 차지한다." (접혀 있어도 47px + 간격)
- 주소가 저장돼 있으면 `.anima-srv-card` 를 **통째로 `#anima-adv-toggle` 뒤로 옮긴다**(상단 130 → 0px).
- ⚠ **`#anima-adv` 의 '안'에 넣으면 안 된다** — `_animaRenderAdv()` 가 `innerHTML` 로 통째로 다시 그려서
  옮겨 둔 카드가 조용히 사라진다. **토글의 형제**로 둬야 살아남는다(`srvcard-test` 가 이걸 본다).
- ⚠ **주소가 없으면(첫 설치) 맨 위에 그대로 남긴다.** 안 그러면 주소 넣을 곳을 못 찾아 앱을 못 쓴다.
  이 조건 분기는 설치 때 한 번뿐이라 '쓰는 도중 화면이 튀는' 부류가 아니다.
- 고급 설정을 여닫을 때 이 카드도 함께 여닫는다(`.anima-srv-card.in-adv`).

검사: `srvcard-test`(첫 설치 잔존 · 실사용 0px · 첫 카드가 「이미지 넣기」 ·
**고급 설정을 다시 그려도 살아남는가** · 여닫기).

## 🔗 공유는 '새로고침'이 아니라 **새 문서**다 — 보던 화면을 되돌려 놓는다 (v9.176.0 — 사용자 신고)

사용자 신고: **"결과를 크게 본 채로 다른 앱에서 이미지를 공유하면 보던 화면이 바뀐다."**

원인은 구조다. 안드로이드 공유는 **POST 내비게이션**이고(`manifest.json` 의 `share_target`),
`sw.js` 가 그 POST 를 받아 이미지를 캐시에 넣은 뒤 **`Response.redirect('./index.html?shared=1', 303)`**
한다. 즉 '새로고침 같은 느낌'이 아니라 **진짜로 앱이 통째로 다시 뜬다.**
`sw.js` 는 열려 있는 창을 재사용하지 않는다(`clients.matchAll` 은 업데이트 알림용뿐).

**새로고침 자체는 못 없앤다** — 웹 표준상 문서를 하나 돌려줘야 한다.
(SW 가 기존 창에 `postMessage` 하고 공유 창을 `window.close()` 하는 우회가 있지만,
PWA 창에서 close 가 안 될 때가 있어 빈 화면이 남는다. 안 쓴다.)
→ 그래서 **'보이는 것'을 되돌려 티가 안 나게** 한다.

- `_animaSaveLbResume()` 이 `visibilitychange(hidden)`·`pagehide` 에서 보던 자리를 적는다
  (결과 키 `k` · 폴더 · '제일 최신을 보고 있었나' · 시각). 키 `anima_lb_resume_v1`, TTL 15분.
  ⚠ **그때뿐인 상태라 `_IO_ETC_KEYS`(백업)에는 일부러 안 넣는다.**
- `_animaResumeLb()` 는 **`?shared=1` 로 들어왔을 때만** 되살린다. 평소 진입에 열리면
  "안 열었는데 열린다"가 된다. 결과 목록은 IndexedDB 에서 비동기로 오므로 최대 4초 기다린다.
- 크게 보기를 **닫고** 나갔으면 적어 둔 것을 지운다 → 공유로 들어와도 안 열린다.

### ⚠ 머리 자리(`_lbHead`)는 `_lbLive()` 가 `headIdx` 를 돌려줘야 작동한다
`_lbRefresh` 는 `head == null` 이면 머리 자리 규칙을 **아예 안 쓴다.**
그래서 `window._lbHeadPending = 0` 만 세워서는 안 되고, `_lbLivePending()` 이
`headIdx: 0` 을 함께 실어 보내야 한다(갤러리는 새것이 맨 앞).
- `_animaOpenLbAt(i, sitAtHead)` 하나로 **탭해서 열 때와 공유 복원**이 같은 길을 쓴다.
  `sitAtHead` 일 때만 `headIdx` 를 싣는다 — 평소 탭 동작을 예전 그대로 두기 위해서다.
- 보던 게 제일 최신이었으면 머리 자리에 앉혀 **공유 뒤 자동 생성 결과가 그 자리에 갈아끼워진다.**
  옛 결과를 보고 있었으면 안 앉힌다(그 그림을 계속 봐야 한다).

### ⚠ `?shared=1` 의 레이아웃 강제는 건드리지 말 것
공유 진입은 `pl_layout` 을 **'anima' 로 저장**한다(`index.html` head 부팅 스크립트 + `_plShareInit`).
저장을 빼면 클래식 사용자가 공유할 때 깨진다 — `11250`(`initApp` 호출) · `23810`(body 에 data-layout) ·
`26201`(클래식 엔진 적재) · `27384` 가 전부 **`?shared=1` 을 안 보고 localStorage 만 읽기 때문**이다.
특히 `23810` 이 body 에 'classic' 을 걸면 **body 가 html 을 이겨서**(테마 변수 항목과 같은 함정)
공유인데 클래식이 뜬다. 고치려면 네 곳을 함께 손봐야 한다 — 한 줄짜리가 아니다.

### ⚠ '앱을 벗어남' 신호는 **화면 반반(멀티윈도우)에서는 안 온다** (v9.180.0 — 사용자 신고)
사용자 실사용: 폴드8을 반반으로 나눠 **오른쪽 프롬프트 랩(크게 보기) · 왼쪽 갤러리**를 띄우고 공유한다.
이때 프롬프트 랩은 계속 **보이는** 상태라 `visibilitychange(hidden)` 도 `pagehide` 도 안 온다
→ 보던 자리를 적어 둘 기회가 없어 v9.176.0 의 복원이 통째로 안 먹었다.
→ **크게 보기가 바뀔 때마다(`_updateUI`) 그때그때 적는다.** '나갈 때 적는다'에 기대지 말 것.
⚠ `window._lbActive` 를 `_updateUI()` **뒤에** 켜면 첫 저장이 건너뛰어진다 → 먼저 켠다.

## 🪟 2단 여부는 **`data-two` 한 곳**이 정한다 — 사용자가 고른다 (v9.184.0)
사용자 신고: "태블릿에서 2단으로 보이는데 풀 수가 없다." **화면 크기로 의도를 맞히려다 세 번째**다
(v9.174.0 2단 문턱 · v9.175.0 레일 조건 · 이번).
- 메뉴 → 화면 → 「화면 나누기」 가 **자동 → 항상 2단 → 항상 1단** 으로 돈다. 키 `anima_twocol_v1`
  (`_IO_ETC_KEYS` 등록). 기본은 `auto` 라 예전과 똑같이 동작한다.
- ★ **CSS 를 미디어 쿼리에서 `#anima-root[data-two="1"]` 로 바꿨다.** 예전엔 JS 판정(`_animaTwoCol`)과
  CSS 미디어 쿼리를 **글자까지 똑같이** 적어야 했고 어긋나면 조용히 샜다 — 이제 근거가 한 곳뿐이다.
  `_animaSyncTwoCol()` 이 '설정 + 폭'을 보고 `data-two` 를 걸고, CSS 는 그것만 본다.
- ⚠ 마운트 때 **첫 페인트 전에** `data-two` 를 건다(안 그러면 1단으로 떴다가 2단으로 튄다).
- ⚠ 리사이즈에서 `_animaSyncTwoCol()` 을 부르고, **바뀌었을 때만** 다시 그린다.
검사: `twomode-test`(태블릿에서 풀림 · 폰에서 켜짐 · 새로고침 유지 · 메뉴 상태 표시 ·
**CSS 가 data-two 를 보는가** · 백업 키).

## 📥 공유 보관함은 **한 칸이면 안 된다** (v9.183.0 — 사용자 신고)
`sw.js` 가 공유를 늘 같은 칸(`./__shared_image__`)에 덮어썼다. 공유는 매번 **새 문서**를 띄우므로
빠르게 두 번 공유하면: A 저장 → 문서 A 뜨는 중 → B 저장(A를 덮어씀) → 문서 A 가 뒤늦게 읽고 **지움**
→ 문서 B 는 빈 손. **한 장이 통째로 사라졌다.**
- 이제 `_shareSlot()` 이 `./__shared_image__/<시각>-<무작위>` 로 **공유마다 자기 칸**을 쓴다.
- 앱은 `cache.keys()` 로 **밀린 것을 전부** 모아 **오래된 순서대로**(칸 이름에 시각이 들어 있다) 처리하고 비운다.
  옛 서비스워커가 남긴 한 칸짜리도 같은 정규식(`/__shared_image__/`)에 걸려 함께 읽힌다.
- ⚠ **한 장 넣고 곧장 다음 장을 넣으면 안 된다** — 앞 장은 원본만 바뀌고 안 뽑힌 채 사라진다.
  `_plConsumeShared` 가 `await` 로 한 장씩 '넣기 → 보내기'를 끝낸 뒤 다음으로 간다.
- ⚠ `_animaShareAutoGen` 의 '대기열 있으면 건너뛰기' 가드가 **두 번째부터 전부 걸렸다.**
  연속 공유는 '뽑아 달라'는 뜻이 분명하므로 `queueOk` 로 그때만 예외를 준다.
검사: `sharequeue-test`(3장 유실 0 · 순서 · 칸 비우기 · 가드 예외).

## 🖼 크게 보기 목록 = [원본] + [그 원본 결과] + [나머지 최근 결과] (v9.182.0 — 사용자 요청)
예전엔 `_lbBuild()` 가 **그 원본 것만** 담아서, 1장 뽑았으면 `2/2` 로 오른쪽 끝이 막혔다.
→ 그 묶음이 끝나면 최근 결과 전체로 이어붙인다. **원본은 계속 왼쪽 끝**(앞으로는 안 붙인다).
- ⚠ 이미 담은 그림은 `Set` 으로 건너뛴다 — 안 그러면 그 원본 결과와 잠근 그림이 두 번 나온다.
- 이어붙은 구간은 캡션에 **「다른 원본」** 을 붙여 경계가 보이게 한다(숫자만 커지면 어디서부터
  남의 것인지 알 수 없다).
- `genIdx`·`leftIdx`·`headIdx` 는 앞부분 기준이라 그대로 둔다 — 새 결과가 그 자리에 갈아끼워지는
  동작은 유지된다.
- ⚠ **이건 예전 결정을 의도적으로 뒤집은 것이다.** `lbgrp-test` 의 「그 원본으로 뽑은 것만
  들어간다」가 옛 동작을 못 박고 있었다 → '앞쪽 차례는 그대로 + 뒤에 이어진다'로 바꿔 썼다.
  검사를 통과시키려 고친 게 아니라, **바뀐 사양을 검사에 반영**한 것이다.

## 🗑 되돌릴 수 없는 삭제에는 반드시 확인을 붙일 것 (v9.181.0 — 사용자 피해)
「전체 비우기」가 **확인 없이 한 번에** `_anima.results` + IndexedDB + 썸네일 캐시를 지웠다.
사용자가 잘못 눌러 **결과 목록이 통째로 사라졌다.** 되살릴 방법이 없다.
- 지금은 `showConfirm` 으로 **장수**와 **되돌릴 수 없음**을 알리고, **PC 의 원본 파일은 남는다**는
  사실까지 함께 말한다(그래야 '완전히 잃었다'고 오해하지 않는다).
- **다른 파괴적 동작을 만들 때도 같은 기준을 적용할 것.** 토스트로 알리는 건 확인이 아니다.

## ⚠ '제일 최신을 보는 중'은 자리가 아니라 **그림으로** 판단한다 (v9.181.0 — 사용자 신고)
공유 복원에서 `head` 를 `_lbIdx === 0` 으로 판단했는데, **비교 프레임으로 열면 목록이
[원본, 결과] 라 결과가 1번 자리**다 → 늘 `head=false` 가 되어 공유 뒤 새 결과가 그 자리에
안 들어오고 옆으로 밀렸다(사용자: "2/2 였는데 1/65 가 되고, 생성되면 2/66").
→ `_anima.results[0].k === rec.k` 로 **그림 자체**를 비교한다.
⚠ 공유하면 원본이 새 이미지로 바뀌므로 예전 '원본+결과' 묶음은 복원할 수 없다.
  갤러리 범위로 여는 것이 맞고, 중요한 건 **보던 그림이 새 결과로 갈아끼워지는 것**이다.

## ↩ 뒤로가기 문지기 — 앱이 한 번에 꺼지던 것 (v9.180.0 — 사용자 신고)
아무것도 안 열린 상태의 뒤로가기는 히스토리에 돌아갈 곳이 없어 **크롬 창이 그대로 닫혔다.**
부팅 때 `{_plGuard:1}` 항목을 하나 쌓아 그 한 번을 대신 먹고 안내만 띄운다.
- 브라우저는 스크립트로 창을 못 닫는다 → **'두 번째'는 문지기를 다시 세우지 않는 것으로 통과**시킨다.
  2.2초 뒤에 다시 세운다(바로 세우면 영영 못 나간다).
- ⚠ 문지기는 `popstate` 처리의 **맨 끝**(`!_modalBackStack.length`)에서만 작동한다.
  라이트박스(`window._lbActive`)·모달이 열려 있으면 그 앞에서 이미 `return` 하므로 안 끼어든다.
  **이게 제일 중요하다** — 가로채면 "뒤로가기로 창이 안 닫힌다"는 더 나쁜 문제가 된다.
- ⚠ 부팅 시 세우는 자리는 `_plShareInit` **뒤**여야 한다(거기서 `?shared=1` 을 `replaceState` 로 지운다).
  `_plShareInit` 은 중간에 `return` 하는 갈래가 여럿이라 그 안에 넣으면 안 세워질 수 있다.
검사: `backguard-test`(한 번 눌러도 안 꺼짐 · 안내 · 재무장 · **라이트박스/모달은 예전 그대로**).

검사: `shareresume-test`(크게 보기 → 앱 이탈 → `?shared=1` 재진입 → 그 결과가 그대로 열리는가 ·
**새 결과가 그 자리에 실제로 갈아끼워지는가(그려진 src 로 확인)** · 닫고 나가면 안 열림 · 평소 진입엔 안 열림).

## 🖼 크게 보기 '머리 자리'를 스튜디오 갤러리에도 (v9.189.0 — 사용자 요청)

사용자 신고: "스튜디오에서 **1/5 를 보는데 새 이미지가 생성되면 2/6 으로 밀린다.**
Anima 편집처럼 **1번 자리에 새 이미지가 뜨게** 하고 싶다."

머리 자리 규칙(`_lbHead`)은 이미 있었는데 **스튜디오 갤러리가 안 쓰고 있었다.**
`openLightboxFromEl` 이 `_lbParent`(DOM 다시 훑기) 폴백만 쓰고 `headIdx` 를 안 넘겼다.

- 맨 앞(`idx === 0`)을 열었을 때만 `_lbHeadPending = 0` + `_lbLivePending`(headIdx 0 포함)을 건다.
- ⚠ **`_lbHeadPending` 만 세우면 안 된다** — `_lbRefresh` 는 `_lbLive()` 가 `headIdx` 를 함께
  실어 보내야 머리 자리 규칙을 쓴다(v9.176.0 에서 이미 겪은 함정인데 여기서 또 나왔다).
- ⚠ **옛 이미지를 열었으면 아무것도 걸지 않는다** — 그 그림을 계속 봐야 한다.
  머리 자리를 떠나 있는 동안도 마찬가지고, 맨 앞으로 돌아오면 규칙이 되살아난다(`wasOnHead` 판정).

### 어느 컨테이너에 거는가 — 근거는 '삽입 방향'이다
`_lbFrontGallery()` 가 정한다. **새 이미지가 맨 앞에 들어오는 곳만** 대상이다:
- `#result-gallery-grid` (`grid.prepend`) · `#comfy-gallery` (`gallery.prepend`) → 머리 자리 **0**
- `#comfy-live-thumbs` · `#comfy-inline-thumbs-pos/neg` 는 `appendChild`(뒤에 붙음)라 **자리가 안 밀린다** → 제외
**삽입 방향을 바꾸면 `_lbFrontGallery()` 도 함께 바꿔야 한다.**

### 같이 막은 구멍 — 완료된 이미지가 크게 보기에 안 들어왔다
최종 저장 경로가 `gallery.prepend(...)` + `galleryAddImage(...)` 를 하고도 **`_lbRefresh()` 를 안 불렀다.**
`_lbRefresh` 는 `_comfyStageImages` 안에만 있었는데, 웹소켓 단계 이벤트를 받으면(`sawStage`)
그 길을 안 탄다. 머리 자리를 만들어도 갱신이 안 불리면 무용이라 여기에 한 줄 넣었다.

검사: `lbhead-test`(자리 안 밀림 · **그려진 src 가 실제로 새 이미지** · 연속 도착 · 옛것 열면 유지 ·
넘겨 놓으면 유지 · 맨 앞 복귀 시 재작동 · 뒤에 붙는 컨테이너 무손상 · 완료 경로가 갱신 호출).
⚠ **카운터를 `textContent` 로 검사하지 말 것** — 숫자가 굴러가는 오도미터라 자릿수 전체가 나온다
  ("112 / 6"). 위치는 `_lbIdx`, 총장수는 `.lb-total` 로 본다.

## 🎲 추천 축을 '생성할 때마다' 굴린다 (v9.188.0 — 사용자 요청)

스튜디오·클래식의 **「추천」 패널** 축(화풍·인원·인물·의상·노출·구도·배경) 머리에 🎲 를 달았다.
켠 축은 **생성 누를 때마다 담긴 것 자체가 갈아끼워진다.** 키 `rec_roll_axes_v1`(`_IO_ETC_KEYS` 등록 완료).

- ⚠ **기존 🔄(다른 제안)과 뜻이 다르다.** 🔄 는 *보여 주는 후보 12개*만 새로 뽑고 담긴 것은 그대로다
  (그래서 의상을 바꾸려면 담은 걸 빼고 새로 눌러야 했다 — 이 기능이 나온 이유). 두 버튼을 합치지 말 것.
- **노출 축(`custom`)은 굴리지 않는다** — 부위별 토글 패널이라 무작위로 고를 단위가 아니다.
- **「빠르게 생성」은 누를 때 한 번** 굴리고 그 장들이 함께 쓴다(사용자 결정).

### 만들면서 실제로 밟은 지뢰 3개 — 전부 '조용히' 샜다
1. **`_plCurrent` 로 레이아웃을 판정하면 안 된다.** 그 변수는 지연 실행되는 클래식 엔진 블록에서
   **닿지 않는 스코프**에 있어 `ReferenceError` 가 난다. try 로 감싸 두면 **아무 말 없이 영영 안 굴러간다**
   (콘솔도 깨끗하다). → `document.body.getAttribute('data-layout')` 을 본다(24293·27886 에서 걸어 준다).
2. **'뺄 것'을 `_recPickedSet` 하나에만 기대면 쌓인다.** 그 집합은 프리셋 태그가 하나라도 어긋나면
   스스로 항목을 지운다(`_recReconcilePicked` — 유령 선택 방지용이라 그 동작은 맞다). 그러면 다음
   굴리기가 뺄 대상을 못 찾아 **갈아끼우기가 아니라 덧붙이기**가 된다.
   실측: 추천 패널을 안 연 채 12번 굴리니 배경 태그가 **7개까지** 늘었다.
   → 축마다 `_recRollLast` 로 '직전에 굴려서 담은 것'을 따로 기억해 **합집합**으로 뺀다.
   ⚠ 추천 패널을 한 번도 안 열었으면 `_recPickedSet` 이 비어 있다 → 첫 굴리기 직전에 딱 한 번
     `_recBootstrapPicked()` 한다(반복 호출은 유령 선택을 만든다).
3. **`_umoBatch` 로 감싸야 `final-positive` 가 전송 전에 동기로 최신이 된다.** 안 그러면 굴린 게
   이번 장엔 안 먹고 다음 장부터 먹는다 — 제일 알아채기 어려운 부류다.

### 축 목록은 `_recAxes()` 한 곳이다
예전엔 `renderRecommendInto` 안에만 있었다. 굴리기가 자기 목록을 따로 가지면
'완성 모드를 껐는데 인물이 계속 굴러간다' 가 조용히 샌다 — 이 문서에 반복된 그 부류다.
⚠ `_recAllAxisData()` 와 헷갈리지 말 것: 저건 **표시 여부와 무관한 전체**(교차 오염 계산용),
`_recAxes()` 는 **지금 화면에 실제로 있는 축**이다. 굴리기는 보이는 것만 굴린다.

⚠ **이미지 변환·인페인팅에서는 굴리지 않는다.** 그쪽 「자동 생성」도 `comfyGenerate` 를 타는데,
원본을 분석해 채운 태그를 무작위로 갈아끼우면 변환이 통째로 어긋난다.

검사: `recroll-test`(🎲 유무 · 노출 축 제외 · 재렌더 없이 표시만 · **20번 굴려도 한 벌** ·
**패널 안 열고 12번 굴려도 한 벌** · 전송 전 동기 반영 · 「생성 전송」 실제 굴림 · 중복 클릭 ·
안 켜면 무손상 · 화면에 없는 축 제외 · 이미지변환 제외 · 새로고침 유지 · 백업 키).
⚠ **태그 개수로 '쌓임'을 판정할 땐 상한을 눈대중하지 말 것** — 프리셋 하나가 **7태그**짜리도 있다
  (배경 「아늑한 침실 밤」). 임의로 6을 넣었다가 정상인데 실패했다. 그 축의 실제 최대 태그 수를 재서 쓴다.

## ✋ 프리셋 문구는 함부로 건드리지 않는다 (v9.130.1 확정)

**기본 원칙: 지금 잘 나오고 있는 프리셋은 손대지 않는다.**
"더 좋아질 것 같아서" 멀쩡한 문구를 갈아엎지 말 것. 사용자가 이미 그 결과에
익숙해져 있고, 바꾸면 전에 되던 게 안 되기 시작한다.

### 고쳐도 되는 경우 — 딱 이것뿐
- 사용자가 **"이건 안 나온다 / 이상하다"고 직접 말한** 프리셋
- 사용자가 **명시적으로 바꿔 달라고 요청한** 프리셋

그 외에는 **읽기만 하고 그대로 둔다.** 옆 프리셋을 고치는 김에 묶어서 손보는 것도 금지.

### 고칠 때의 방식 (자연어 서술 + 태그 병행)
안 되는 게 확인된 프리셋만 아래 방식으로 다시 쓴다.

**⚠ "클래식은 문장이 안 먹힌다"가 아니다.** 갈리는 건 테마가 아니라 **물린 체크포인트**다.
사용자가 클래식에서 직접 문장을 써서 잘 나오는 걸 확인했다(2026-07-30).

| 물린 모델 | 텍스트 인코더 | 문장 | 태그 |
|---|---|---|---|
| **Anima** | Qwen | ◎ 제일 셈 | ○ |
| **Pony Realism 등 실사 머지** (클래식·스튜디오) | SDXL CLIP + 실사 머지 | **○ 잘 먹힘** | ○ |
| Illustrious / NoobAI / WAI (클래식·스튜디오) | 부루 학습 CLIP | △ 약함 | ◎ |

- CLIP의 진짜 제약은 "문장이냐 태그냐"가 아니라 **앞 75토큰이 제일 세다**는 것이다.
  긴 문장을 맨 앞에 두면 그 문장이 토큰 예산을 먹어 뒤 태그가 약해진다. 문장 자체는 정상 작동한다.
- **문구 순서 판단 기준 = 그 프리셋이 주로 어느 모델에서 쓰이는가.**
  - Anima 프리셋(`ANIMA_DEFAULT_SNIPPETS`) → **서술이 앞**, 태그가 뒤. (Qwen)
  - 클래식 조합(`_PACK_COMBOS`)은 부루 모델에서도 쓰이므로 **태그를 앞**에 둬 예산을 아끼고 서술을 뒤에.
    실사 머지 전용으로 쓸 게 확실하면 서술을 앞으로 빼도 된다 — 금지가 아니다.
- 태그를 **없애지 않는다** — 서술과 **병행**한다(모델에 따라 잘 읽는 쪽이 다르다).
- 단부루 0건 태그라고 지우지 않는다. 0건은 *부루 학습 모델에서 약하다*는 뜻일 뿐,
  실사 머지(Pony Realism 등)는 영어 구절을 그대로 읽는다. (v9.122.0에서 지웠다가 v9.126.0에서 전부 되돌린 전례)
- **자연어 서술 안에는 쉼표를 쓰지 않는다.** 계층·조합이 쉼표로 태그를 쪼개서 문장이 조각난다.
- 기존 사용자 저장분을 올릴 때는 **✏️로 직접 고친 문구는 반드시 보존**한다
  (`INK_OLD` / `INK_OLD_NL` 처럼 '옛 기본 문구와 일치할 때만' 교체).
- 긴 서술이 캡슐로 들어가는 경로면 `_NL_KO`에 짧은 한글 이름을 등록한다.

### ⚠ '원본에 없는 부위'는 색 지시를 빨아들인다 (v9.168.0 — 사용자 실측)
Cosmos Reference는 **보이는 몸을 원본으로 못 박는다.** 반면 후타처럼 원본에 없는 것은
**새로 그리는 자유 구역**이다. 그래서 부위를 대지 않은 색 지시("her skin ... across her
entire body")를 넣으면 **몸은 그대로인 채 자지만 그 색이 됐다.**
- 처방 ① 문구가 **부위를 하나하나 부르게** 한다(얼굴이 제일 센 닻이다).
- 처방 ② 그 자유 구역에도 **못을 박는다** — 자지 색을 안 고르면 조립 시
  "her shaft and testicles are exactly the same skin tone as the rest of her body"를 붙인다.
  사용자가 자지 색을 직접 골랐으면 건드리지 않는다.
- **원본에 없는 부위를 만드는 축(후타 등)을 새로 만들 때 같은 함정을 확인할 것.**

### ⚠ 피부색 성공률은 **투명 배경을 무슨 색으로 채우는가**에 크게 좌우된다 (사용자 실측 2026-08-26)
사용자가 직접 뽑아 본 결과: 투명 PNG 를 **흰색**으로 평탄화하면 피부색이 거의 다 붙고,
**회색·검정**이면 실패가 잦다.
- 기본값은 원래부터 `flattenBg: 'white'` 다. 그런데 **한 번 바꾸면 저장분에 남는다**(`flattenBg` 가
  `_animaSave` 목록에 있다) → 모르는 채로 계속 실패할 수 있다.
- v9.185.0 부터 배경 색 고르는 자리에 실측 사실을 적어 두고, 흰색이 아니면 경고색으로 알린다.
- ⚠ 이것이 `refW` 값을 대신하지는 않는다 — 배경이 흰색이어도 안 붙는 경우가 남는지는 아직 실측 전이다.
  참조 강도(`skin` 의 `refW: 0.6`)는 여전히 임시값이다.

### ⚠ 붙는 순서 = 프롬프트 안의 '자리' (v9.167.0)
넓은 축(피부색·수영복 자국)이 부위 묘사 뒤에 깔리면 원본에 밀려 **들쭉날쭉**해진다.
`_animaGenerate` 가 보낼 때마다 `ons` 를 정렬해 넓은 축을 앞으로 올린다.
⚠ **기본값 배열을 재배치해서는 못 고친다** — 그 순서는 사람마다 다른 *저장분* 순서다.

### 지금까지 자연어 재작성이 적용된 범위
- **피부색(`skin`) 4종** (v9.168.0 — 위 '색 지시를 빨아들인다' 참조) · **문신(`group:'tat'`) · 임신(`group:'preg'`)** (v9.128.0 / v9.129.0) · **음모(`pubic`) · 겨털(`armpit`)** (v9.160.0 — 사용자가 "지저분+희미" 직접 확인, 머리색 연동 + refW 0.85/0.8/0.7)
- 표정·포즈·가슴·후타·노출 등 **나머지는 손대지 않았다.** 문제 제기가 있기 전까지 유지.

### 문신은 v9.150.0에서 사용자 요청으로 전면 교체됐다
사용자가 "종류만 난잡하고 실제로 쓰는 건 바코드·자궁뿐"이라며 직접 갈아엎기를 요청했다.
- **문구 무손상 3개**: `tat_barcode` · `tat_pubic` · `tat_max`. 특히 **`tat_max`(★ 극한 도배)는
  0건 태그가 섞여 있어도 정리하지 않는다** — 사용자가 명시적으로 금지했다.
- 지운 12개는 `TAT_GONE`(id → 옛 기본 문구) 로 마이그레이션한다. **문구가 옛 기본값과 정확히
  같을 때만** 지운다 — ✏️로 고친 건 남는다.
- 새 축: `tatfil`(부위별 장식) · `tatden`(밀도). 새 그룹을 만들면 **세 곳**에 등록해야 한다 —
  마이그레이션 주입 목록(`~15808`), 옵션 패널 줄 배열(`~17662`), `known` Set(`~17677`).
  마지막 걸 빠뜨리면 그 칩들이 **'기타' 줄로 새어 나온다**(후타가 샜던 전례).

### ⚠ 새 프리셋은 '주입 목록'에 그 `group` 이 있어야 기존 사용자에게 간다
마이그레이션은 **주입 목록(`~15806`)에 적힌 group 만** 훑어 새 항목을 붙인다.
거기 없는 group 에 프리셋을 추가하면 **새로 설치한 사람에게만 보이고** 이미 쓰던 사람에겐
영영 안 나타난다. v9.152.0에서 `pose` 가 그랬다(그동안 포즈는 추가돼도 안 들어갔다).
새 프리셋을 넣으면 그 group 이 목록에 있는지부터 확인할 것.

### `group` 은 기본이 '하나만 켜짐' — 여러 개 켜려면 `multi: true`
`_animaToggleSnip` 은 같은 `group` 의 다른 항목을 끈다. 부위별 문신처럼 **동시에 켜져야 하는**
축은 `multi: true` 를 준다. 끄는 쪽도 `multi` 를 보므로, 한 그룹 안에 배타 항목과 다중 항목을
섞어 둬도 서로 간섭하지 않는다.

### ⚠ 'group 이름을 나열하는 코드'는 축이 늘면 조용히 샌다 (v9.156.0 전체 검사에서)
축을 새로 만들 때마다 이름을 더해야 하는 곳이 여럿인데, 빠뜨려도 아무 오류가 안 난다.
전체 검사에서 실제로 이렇게 새고 있던 것들:
- **참조 강도 자동 하향**이 `group === 'tat' || 'preg'` 만 봐서, `tatfil` 에 적어 둔 `refW: 0.7` 이
  통째로 무시됐다 → 장식 문신이 원본에 눌렸다. **지금은 group 을 안 보고 `refW` 선언 여부만 본다.**
- **`_animaOptLabel`** 이 6개 group 만 이름을 대서 v9.146.0·v9.150.0 에서 늘어난 축이 캡션에서
  전부 빠졌다 → 그 축만 바꿔 뽑으면 결과가 전부 같은 이름. **지금은 '이름 안 댄 나머지'를 한꺼번에 담는다.**
- **마이그레이션 주입 목록**에 `pose` 가 없어 새 포즈가 기존 사용자에게 영영 안 갔다(v9.152.0).
- **`known` Set** 에 새 group 이 없으면 그 칩이 '기타' 줄로 샌다(v9.150.0).

**가능하면 이름 나열 대신 '속성으로 판정'하도록 짤 것.** 나열이 불가피하면 새 축을 만들 때
위 네 곳을 전부 확인한다.

### ✅ v9.165.0부터 축 나열은 `_ANIMA_SECS` 한 곳으로 모았다
옵션 패널의 6덩어리 정의(`_ANIMA_SECS`)가 **세 곳을 동시에** 정한다:
- **옵션 패널 섹션**(`_animaPanelHtml`) · **하단 바 그룹 버튼**(`_ANIMA_GRP` / `_ANIMA_GRPBTNS` / `_animaGrpPopHtml`)
- **'기타' 줄 판정**(`_ANIMA_SEC_GROUPS` → `_animaEtcList`) — 옛 `known` Set 을 대체했다.

**새 축을 만들면 `_ANIMA_SECS` 의 `flds` 에 한 줄만 더하면 세 곳에 전부 반영된다.**
(여전히 따로 확인해야 하는 곳: **마이그레이션 주입 목록**(`~15850`) — 기존 사용자에게 가는 경로.)

칸(fld) 속성: `g`(그룹 이름) · `pick`(그룹 없는 항목) · `futa`(후타 켰을 때만) ·
`fold`(표정·포즈의 '최근+더보기') · 덩어리 속성 `adult`(성인 표시 꺼지면 통째로 숨김).

### ⚠ 패널은 '다시 그리지 않는 경로'가 있다 — 요약 줄도 따라가게 해야 한다
`_animaToggleSnip` 은 눌림 모션을 지키려고 패널을 통째로 다시 그리지 않는다. 그래서
**요약 줄('지금 켜진 것')과 덩어리 머리글의 '2개 켜짐' 배지**는 `_animaSyncPanelMeta()` 로
따로 갱신한다. 패널에 '상태에 따라 달라지는 표시'를 새로 넣으면 여기도 같이 손봐야 한다
(`_animaSyncChipStates` · `_animaSyncGrpBtns` · `_animaSyncReposeWarn` 과 같은 부류).

### ⚠ 칩 폭은 실측으로 정해져 있다 (390px)
`.anima-sec-b` 좌우 여백 **8px** + `.anima-fld-i` 간격 **5px** + `.anima-chip.tight` 패딩 **6px 9px**
= 가슴 6단계(절벽~초폭유)가 301/310px 로 **딱 한 줄**에 들어간다. 여기 숫자를 올리면 다시 두 줄이 된다
(사용자가 "딱 하나 때문에 2줄로 밀린다"고 지적한 그 문제). 바꾸려면 반드시 실제로 재 볼 것.

### ⚠ 프리셋을 지울 땐 '그동안의 모든 옛 기본 문구'를 넣어야 한다
`TAT_GONE` 이 v9.128 자연어판 문구 하나만 들고 있어서, **그 전 태그판을 쓰던 사람에겐
아무것도 안 맞아 지운 문신이 목록에 영영 남았다.** 값은 배열로 두고 `INK_OLD` · `INK_OLD_NL`
에 있는 옛 판까지 전부 넣는다. ✏️ 보존 원칙은 그대로(어느 옛 기본값과도 안 맞으면 남긴다).

### ⚠ 프리셋 플래그는 **저장·복구 목록 두 곳**에 넣어야 살아남는다 (v9.153.0)
`_animaSave()` 의 `p.snippets = ...map(...)` 와 `_animaRestore()` 의 `s.snippets.map(...)` 은
**적힌 필드만** 옮긴다. 여기 없는 플래그는 저장 한 번에 **통째로 사라진다.**
- `multi` 가 빠져 있어서 장식 문신이 **켠 직후엔 멀쩡하다가 새로고침하면 하나만 켜졌다.**
  (v9.117.1 의 `nsfw` 누락과 똑같은 사고 — 그때도 성인 항목이 새어 나왔다)
- 이미 저장된 사람 것까지 되살리려면 **복구 때 id로 기본값에서 되찾아 준다**
  (`ANIMA_DEFAULT_SNIPPETS.find(v => v.id === x.id)`). 목록에 넣는 것만으로는 기존 저장분이 안 고쳐진다.
- ⚠ **검사는 반드시 새로고침을 끼워서** 한다. 켜자마자 확인하면 이 부류는 전부 통과한다.
  `share-test` 는 새로고침 뒤에 **하나 더 눌러** 앞의 것이 꺼지는지 본다.

## 🔗 공유 진입은 '실행 버튼을 대신 눌러 주는 것'이다 (v9.153.0)

`_animaShareAutoGen()` 은 예전에 노출을 `base_full` 로 **강제**하고 표정·포즈·가슴을 **꺼 버렸다.**
그래서 공유로 넣으면 맞춰 둔 설정이 무시되고 노출·후타만 반영됐다.
사용자 요청으로 그 손질을 걷어냈다 — 지금은 **켜 둔 설정 그대로** 한 장 뽑는다.
건드리는 게 없으므로 되돌리는 코드도 없다. 대기열이 차 있으면 건너뛰는 것만 유지.

## ⚡ 클래식 엔진은 지연 실행된다 (v9.135.0)

`index.html` 안 `<script type="text/plain" id="pl-classic-engine">` 블록(637KB)에
클래식 계열 코드(태그 DB·에디터·조합·추천·DB 관리 등)가 **문자열로** 들어 있다.
`_plEnsureClassic()` 이 `<script>` 엘리먼트에 넣어 **전역 스코프에서 동기 실행**한다.

- 클래식/스튜디오/이미지변환/인페인팅으로 들어오면 **부팅 중 즉시** 실행 → 예전과 동일.
- Anima(및 `?shared=1` 공유 진입)면 **아예 싣지 않는다.** 레이아웃 선택기를 열 때
  또는 실제로 클래식 계열로 옮길 때만 싣는다.
  ⚠ **자동 예열(유휴 예열)을 다시 넣지 말 것** — 637KB는 쪼갤 수 없는 통짜 실행이라
  도는 동안 메인 스레드가 통째로 멈춘다. v9.135.0에서 넣었다가 '앱 뜬 직후 1초 렉'으로
  나타나 v9.137.0에서 제거했다.

### 이 블록을 고칠 때 반드시 지킬 것
- **`document.readyState === 'loading'` 을 전제로 한 코드를 넣지 말 것.**
  지연 실행 시점엔 이미 `loading` 이 아니라서 `else`(=즉시) 분기로 들어간다.
  그 즉시 분기가 블록 **아래쪽에 선언된 `let/const`** 를 건드리면 TDZ 오류로 터진다.
  (실제로 `_fixedPrefixInit()` 이 이렇게 터졌다 → `setTimeout(..., 0)` 으로 한 박자 미뤄 해결)
- 블록 안에 `</script>` 문자열을 넣지 말 것(HTML 파서가 거기서 끊는다).
- 블록은 **메인 `<script>` 보다 앞**에 있어야 한다 — `_plEnsureClassic()` 이
  `getElementById` 로 찾기 때문이다. 뒤로 옮기면 조용히 실패한다.
- Anima 쪽 코드가 이 블록의 함수를 부를 수 있으면 `typeof f === 'function'` 로 감쌀 것.
- ⚠ **`#app-container` 바깥 UI(상단바·전역 모달)에서 이 블록의 함수를 부르면 안 된다.**
  상단바는 Anima에서도 보이기 때문에 눌러도 아무 일이 안 일어난다(실제로 v9.135.0에서
  '내보내기/불러오기'가 Anima에서 안 열리게 됐다 → v9.139.0에서 수정).
  꼭 필요하면 그 진입점에서 먼저 `_plEnsureClassic()` 을 부른다.
  지금 그렇게 처리된 곳: `toggleGtMenu()`(상단 메뉴), 상단바 고정 핀 클릭,
  `openNewbieMode()`, `openLayoutPicker()`, `applyLayout()`.
- ⚠ **코드만 싣는 걸로는 부족하다.** 클래식 화면(계층 입력칸·캡슐·사이드바)은 `initApp()`이 만든다.
  Anima로 부팅하면 블록 안 부팅 분기가 `initApp`을 건너뛰므로, 그 화면을 읽는 기능
  (예: 레이어 재배치)이 터진다 → `_plEnsureClassic()` 이 마지막에 `initApp()`까지 부른다.

## 🧷 `renderChips()` 안에서 컨테이너에 `addEventListener` 하지 말 것 (v9.142.0)

`renderChips(n)` 은 `#chip-container-n` 의 **안쪽만** 새로 그린다 — 컨테이너 엘리먼트 자체는
앱이 뜰 때 한 번 만들어지고 계속 같은 것이 쓰인다. 그래서 이 함수 안에서
`container.addEventListener(...)` 를 부르면 **다시 그릴 때마다 핸들러가 한 겹씩 쌓인다.**
오래 쓸수록 한 번 놓았을 뿐인데 옛 목록을 들고 있는 핸들러들이 줄줄이 실행돼
엉뚱한 캡슐이 딸려가거나 Delete 가 여러 번 먹는다. (drop·click·keydown 셋 다 이랬다)

- 컨테이너에 거는 것은 `if (!container.dataset.plBound) { container.dataset.plBound = '1'; ... }`
  안에 넣어 **딱 한 번만** 건다.
- 그 핸들러는 클로저에 잡힌 `rawTags` 를 쓰면 안 된다(옛 값이다) —
  실행 시점에 `textarea.value` 를 **다시 읽는다.**
- 캡슐(`.prompt-chip`) 자체에 거는 건 상관없다. 캡슐은 매번 새로 만들어진다.

## 🔤 자동 정렬을 함부로 끄지 말 것 — LoRA 트리거 핀을 겸하고 있다 (v9.148.0)

`syncFromManualInput()` 의 `sortLayerByDB()` 는 **정렬만 하는 게 아니다.**
그 안에서 `_comfyTrigPins()` 로 **LoRA 트리거 워드를 프롬프트 맨 앞으로 끌어올린다.**

v9.141.0에서 "메모장처럼 쓰고 싶다"는 요청에 맞춰 텍스트 칸 blur 경로를
`keepOrder=true`(정렬 건너뜀)로 바꿨는데, 그게 **트리거 핀까지 함께 건너뛰었다.**
→ 트리거가 친 자리(대개 뒤쪽)에 묻혀 75토큰 앞머리를 벗어나고 **LoRA 가 약하게 걸린다.**
→ 사용자 증상: "얼굴이 달라진다 · 피부질감이 다르다 · 구도가 다르다 · 로라가 가끔 안 먹는다".
   캡슐 경로는 여전히 정렬해서 **간헐적으로 보였다.**

**교훈: 정렬을 끄는 변경은 프롬프트 내용 변경이다.** 순서가 바뀌면 그림이 바뀐다.
- 지금은 `editor_keeporder_v1`(기본 꺼짐 = 예전처럼 정렬) 스위치로 사용자가 고른다.
- `_IO_ETC_KEYS` 에도 등록돼 있다.
- 확인 방법: `keeporder-test`(정렬/유지 양쪽 + 트리거 위치 + 백업 포함)

### ✅ v9.170.0 — 트리거 핀을 '보낼 때' 한 번 더 (메모장 모드에서도 LoRA 가 걸리게)
정렬을 끄면 트리거 핀도 함께 꺼지는 구조 자체가 남아 있었다 → `comfyGenerate` 가
`_comfyPinTriggers(pos)` 로 **보낼 프롬프트만** 손본다. 화면의 계층 글은 안 건드린다
(메모장 모드의 뜻 유지).
- ⚠ **'구조'지 '정렬'이 아니다.** 트리거가 이미 앞머리(`_COMFY_HEAD_CHARS` = 260자 ≈ 75토큰)
  안에 있으면 **아무것도 안 한다.** 무조건 맨 앞으로 올렸더니 `score_9` 보다 앞으로 가서
  **기본 경로의 전송 프롬프트가 예전과 달라졌다** — 사용자가 항의한 "그림이 달라진다"를
  내가 그대로 재현한 꼴이라 되돌렸다.
- 검사: `lorapin-test`(정렬 켬/끔 × 짧은/긴 프롬프트 + **최적화 직전 버전과 payload 글자 비교**).
  비교본은 `git show fd2c8ac:index.html > /tmp/v112app/index.html` 로 만든다(v9.134.0 = 지연 실행 직전).

### ⚠ 스위치는 `syncFromManualInput` 안에서 보면 안 된다 (v9.148.1에서 겪음)
`keepOrder=true` 는 텍스트 칸 blur 말고도 **사용자가 일부러 배치한 경로**가 함께 쓴다:
캡슐을 다른 계층으로 끌어 옮기기(`_chipMoveRanges`) · 같은 계층 순서 바꾸기 ·
전체 모드 **[저장(적은 그대로)]**(`saveFullEditorText`).
함수 안에서 스위치를 물리면 **끌어 놓자마자 DB 순서로 튄다.**
→ `keepOrder` 는 '정렬하지 않는다'는 뜻 그대로 두고, **스위치는 blur 호출부에서만** 넘긴다
  (`onblur="... syncFromManualInput(i, _editorKeepOrderOn())"`).

### ⚠ 드래그·정렬 검사는 **실제 DB에 있는 태그**로 할 것
`aaa, bbb, ccc` 같은 가짜 태그는 `sortLayerByDB` 가 순서를 안 바꾼다(사전에 없어 전부 Infinity).
그래서 **정렬 회귀가 검사에 안 잡힌다** — v9.148.0의 위 사고를 `chipdrag-test` 가 통과시켰다.
`promptDB` 에서 태그를 뽑아 쓰고, 'DB 순서와 반대로' 넣어 확인한다.

## 📐 결과 크기는 노드에게 맡기지 말 것 (v9.151.0)

"결과가 원본보다 살짝 늘어난다"의 원인은 **크기 결정을 ComfyUI 노드에 통째로 맡긴 것**이었다.
`ImageScaleToTotalPixels` 가 목표 화소수에 맞춰 가로·세로를 **각각 따로** 반올림하고,
이어지는 `VAEEncode` 가 다시 **각각 따로** 8의 배수로 잘라낸다. 두 축이 따로 움직이니
한쪽만 몇 px 깎여 **비율이 틀어졌다**. 원본에 따라 세로로도 가로로도 늘어난다(최대 0.7%).
실측: `896x1152 → 896x1160`(-0.69%) · `720x1280 → 768x1360`(+0.39%)

- 지금은 `_animaGenWH()` 가 생성 크기를, `_animaOutWH()` 가 출력 크기를 **앱에서 정해** `ImageScale`로 넘긴다.
- **격자는 16이다** (VAE 8 × Qwen 2x2 패치). 8만 맞추면 한 축이 어긋난다.
- **비율은 배율을 곱해 반올림하면 안 된다** — 그러면 또 0.03% 틀어진다.
  `_animaRatioFit()` 처럼 **최대공약수로 줄인 정수비의 배수**만 쓴다(오차 0).
- `matchSrcSize`(기본 켜짐) 는 저장 직전 노드 `64`로 **원본 크기·비율로 되돌린다.**
- ⚠ **붙여넣은 커스텀 워크플로우(`_animaInjectInto`)는 절대 건드리지 않는다** — 사용자 것이다.
- 검사: `size-test`(실사용 크기 8종 · 비율 0% · 포즈 골격 · 커스텀 워크플로우 무손상)

## 🧯 마이그레이션은 한 단계씩 감싼다 (`_mig`) — v9.169.0

`_animaRestore()` 의 마이그레이션은 **전부 try 하나 안**에 있었다. 그래서 앞 단계가 던지면
뒤 단계가 **통째로 조용히 건너뛰어졌다**(콘솔도 깨끗하다). v9.92.0에 이어 두 번째 사고다.

- 전수 조사에서 재현: **저장분의 `text` 가 문자열이 아니면** `(x.text || '').trim()` 에서 던지고,
  이름 통일·피부색 교체 같은 **뒷단계가 영영 안 간다.**
- 처방 ① 복구 때 **타입을 강제**한다(`_sstr` — id·name·text·kind·group·detail).
  ② 단계마다 **`_mig(() => { ... })`** 로 감싼다. **새 마이그레이션도 반드시 이걸로 감쌀 것.**
  ③ `_animaRestore` 끝에 **`finally` 안전망** — 무엇이 터지든 옵션이 비면 기본값을 깐다
     (저장분 JSON 이 깨지면 첫 줄 `JSON.parse` 가 던져서 **칩이 0개인 빈 화면**이 됐다).
- 검사: `robust-test`(망가진 저장분 12종으로 부팅 → 마지막 단계까지 도는지).

## 🖼 갤러리 썸네일은 '한 번만 받아 캐시에서 꺼낸다' (v9.173.0)

썸네일 주소에는 `preview=webp` 가 붙어 있어 **ComfyUI 가 요청받을 때마다 다시 굽는다.**
그림 만드는 그 PC 의 CPU 고, 폰 원격(Tailscale)이면 회선까지 같이 먹는다. 그래서 요청 수가 곧 비용이다.

v9.172.0 까지 실제로 이랬다(가짜 ComfyUI 로 300장 실측):
- `<img src=주소>` 가 한 번 받고, 캐시를 채우는 `fetch` 가 **또** 받아 **장당 2건**이 나갔다.
- 캐시에 있어도 `_animaThumbSrc` 는 '지금 당장 문자열'을 돌려줘야 해서(칸 높이 0 방지)
  **두 번째 방문에도 네트워크를 다시 탔다.**

지금은 `_animaImgOne()` 하나가 ① 메모리 → ② IndexedDB → ③ 네트워크 순으로 보고,
**받은 건 blob 으로 캐시에 넣고 그 blob 을 화면에 건다.** 120장 기준 두 번째 방문 요청 **0건**.

**고칠 때 지킬 것**
- **칸 크기를 먼저 잡아야 캐시를 쓸 수 있다.** `src` 를 늦게 넣으려면 `.anima-gitem` 에
  `aspect-ratio` 가 먼저 있어야 목록이 안 밀린다. 크기를 아는 결과는 정확히(`r.w`/`r.h`),
  모르면 `_animaArDefault()`(이미 아는 결과의 비율)로 잡고 **처음 뜨는 순간 기억**한다.
- **보고 있는 칸의 비율은 그 자리에서 고치지 않는다.** 눈앞에서 칸이 커졌다 작아지면
  그게 바로 '목록이 밀린다'로 느껴진다. 화면 밖 칸만 지금 맞추고, 보이는 칸은 다음 렌더에서 맞춘다.
- **CORS 가 막히면 `fetch` 는 실패하지만 `<img src>` 는 된다.** 실패하면 반드시 예전 방식으로
  되돌아가야 한다 — 캐시를 못 쓰는 건 참아도 사진이 안 보이면 안 된다.
- **동시 4건(`_ANIMA_IMG_MAX`) 제한을 풀지 말 것.** 갤러리를 훑는 동안 생성 요청이 굶는다.
- 검사: `gallery-test`(요청 수 · 중복 · 두 번째 방문 0건 · 안 밀림 · CORS 차단 · 기능 무손상)

### ⚠ 레이아웃 방식을 바꾸면 **그걸 조작하던 컨트롤도 같이 죽는다** (v9.186.0 — 사용자 신고)
갤러리를 다단(column)에서 **격자(grid)** 로 바꾼 뒤, 칸 폭은 `--gcw` 가 정하고
`column-width` 는 `auto` 로 죽었다. 그런데 크기 슬라이더는 계속 `column-width` 만 바꿔서
**끌어도 아무 반응이 없었다.** 오류도 안 나고 핸들러도 멀쩡히 붙어 있어서 코드만 봐선 정상이다.
- 검사는 '핸들러가 붙어 있는가'가 아니라 **칸이 실제로 커지고 작아지는가**로 봐야 잡힌다
  (`gallery-test` 가 `getBoundingClientRect().width` 로 잰다).
- **레이아웃 방식(그리드↔다단↔플렉스)을 바꿀 때는 그 값을 건드리는 코드를 전부 찾아볼 것.**

### ⚠ 성능 고치려고 HTML 문자열을 새로 쓰면 기능이 조용히 빠진다
`_animaGalCell()` 을 다시 쓰면서 **칸의 ✕(삭제) 버튼을 통째로 빠뜨렸다.** 오류도 안 나고,
CSS·클릭 핸들러는 그대로라 코드만 봐선 멀쩡해 보였고, **28개 검사 전부가 통과했다.**
- 마크업을 갈아끼우는 변경은 **바꾸기 전 문자열과 눈으로 대조**한다(지운 속성·자식 요소 목록).
- 성능 검사에는 **그 화면의 기능이 아직 되는지**를 한 줄이라도 같이 넣는다
  (`gallery-test` 는 ✕ 버튼 개수와 실제 삭제 동작을 본다).

## 💾 백업(내보내기)에 새 설정 키를 빠뜨리지 말 것 (v9.140.0)

`index.html` 의 **`_IO_ETC_KEYS`** 배열에 있는 localStorage 키만 백업에 담긴다.
여기 없으면 사용자가 기기를 옮길 때 **그 설정이 통째로 사라진다.**
실제로 Anima 설정·LoRA 칩·체크포인트 목록·이미지변환/인페인팅 설정이
빠져 있었다(v9.140.0에서 추가).

**새 `localStorage` 키를 만들면 반드시 `_IO_ETC_KEYS` 에도 넣는다.**
검증은 `backup-test`(백업 → 빈 기기 → 복구 → 새로고침 왕복)로 한다.
⚠ v9.169.0 전수 조사에서 또 16개가 빠져 있었다(성인 팩·고정 프리픽스·추천·얼굴 프롬프트·
이미지변환/인페인팅 탭 등). 찾는 법: `localStorage.(set|get)Item('키')` 를 전수로 뽑아
`_IO_ETC_KEYS` + 전용 필드(`promptDB`·`comboFS`·`contextTabs`…) 어디에도 없는 것을 고른다.
그림 데이터(`comfy_img2img_src_v1`·`mg_previews_v1`)와 그때뿐인 상태는 일부러 뺀다.

## 🧑‍🤝‍🧑 사용자 10명 시나리오 검사 (v9.143.0)

기능 단위 테스트만으로는 "메뉴가 열린다"까지밖에 못 본다. 실제 사용 방식 10가지를
처음부터 끝까지 재현하는 검사를 따로 둔다 (`persona/p1.js` ~ `p10.js`).

① 폰 Anima 헤비(결과 500장·공유 진입) ② PC 클래식 장인(계층당 60개·드래그·undo)
③ 기기 이전(백업→빈 기기→복구) ④ 프리셋 40개(175개짜리 적용 성능) ⑤ 태그 DB 관리자
⑥ 실사 머지(문장형 조합) ⑦ 부루 모델(LoRA 4개·75토큰 앞머리) ⑧ 이미지변환/인페인팅
⑨ 원격·오프라인(SW 캐시) ⑩ 첫 사용자(빈 저장소·성인 미동의)

### 검사를 짤 때 조심할 것 (여기서 가짜 실패가 났다)
- **`addInitScript` 로 localStorage 를 심으면 새로고침마다 다시 심긴다.**
  앱이 저장한 값을 되돌려 놔서 "새로고침하면 설정이 안 남는다"는 가짜 실패가 난다.
  `sessionStorage` 플래그로 **첫 진입에만** 심을 것.
- **클래식 화면을 좌표로 조작하려면 화면을 넓게** 잡아야 한다(390px에서는 캡슐이
  가로로 밀려나 `elementFromPoint` 가 null 을 준다).
- **복구(`executeIO`)는 마지막에 앱을 스스로 새로고침한다** — `page.evaluate` 안에서
  기다리면 컨텍스트가 파괴된다. 새로고침을 밖에서 기다린 뒤 확인할 것.
- **성인 조합은 `adult_optin_v1` 만으로 안 나온다.** `adult_pack_v1`(팩 스위치) 또는
  `_catalogInstalledIds`(카탈로그 팩)가 있어야 한다.
- **`중복 감지`는 DB가 아니라 지금 에디터에 적힌 프롬프트를 본다.** DB 중복은 `DB 검사`다.
- 캡슐 글자수로 판단하지 말 것 — 캡슐은 `[한글 이름][영문 원문]` 두 줄이라 항상 길다.

## 📄 참고 문서

- **`REMOTE_ACCESS_GUIDE.md`** — 폰에서 원격으로 쓰는 구성(적용 완료).
  Tailscale 설정, ComfyUI 배치파일 최종본과 그 이유, 원격 PC 종료·예약,
  "폰에서 직접 생성은 왜 불가능한가"(모델 아키텍처 근거)까지 정리돼 있다.
  **원격 접속·배치파일·PC 종료 관련 작업 전에 먼저 읽을 것.**
